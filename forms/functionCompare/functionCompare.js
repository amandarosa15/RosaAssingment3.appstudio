/*
let password=prompt("Enter your new password")
let doublecheck=prompt("Enter your password again")

function Compare(password, doublecheck){
  var result = password.localeCompare(doublecheck)
  return result
}

let TruePassword = Compare(password, doublecheck)

if (TruePassword==1)
  console.log("The passwords are not the same. ")
if(TruePassword==0)
  console.log("The passwords are the same. ")
*/